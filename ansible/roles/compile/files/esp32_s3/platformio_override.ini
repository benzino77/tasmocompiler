[platformio]
default_envs = tasmota32s3

[env:tasmota32s3]
extends = env:tasmota32_base
board = esp32s3-qio_qspi
build_flags = ${env:tasmota32_base.build_flags}
lib_ignore = TTGO TWatch Library, Micro-RTSP, epdiy
